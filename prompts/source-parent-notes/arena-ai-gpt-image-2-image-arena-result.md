# Arena AI GPT-Image-2 Image Arena Result

Source post:
- Account: `Arena.ai @arena`
- Post URL: `https://x.com/arena/status/2046996779418423431`

## Summary

The visible post states that `GPT-Image-2` had a `93%` win rate in `Image Arena`.

The ranking method is described as:

- blind pairwise comparisons
- anonymized outputs
- same prompt comparison

The visible claim is that `GPT-Image-2` from `OpenAI` was preferred `93%` of the time and achieved a record `+242` point jump.

The quoted Arena post also states that it reached the `#1` spot across Image Arena leaderboards.

## Why It Matters

- This is a useful external benchmark signal for `GPT-Image-2`
- It is relevant when comparing real-world preference performance rather than self-reported quality
- It adds context for why so many workflow posts are shifting toward `GPT-Image-2`

## Classification

- Category: `AI technique / benchmark`
- Contains reusable prompt: `no`
